Synthetic WoWAudit API responses. Each one keeps only the fields `wowaudit_web.py` reads, with the
shapes observed read-only on 2026-09-22. Nothing here comes from a real session: no cookies, no
CSRF tokens, no API keys, no battletags. The CSRF token in `characters_page.html` is a placeholder.
